namespace Flux.NewRelic.DeploymentReporter.Models.Flux
{
    public enum Kind
    {
        Unknown = 0,
        GitRepository = 1,
        ImagePolicy = 2,
        // other to be defined
    }
}
